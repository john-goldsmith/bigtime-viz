import $ from 'jquery';
import 'bootstrap'
import 'bootstrap-datepicker';

$('.input-daterange').datepicker({});

// $('#time-range').on('change', event => {
//   window.location.search = `timeRange=${event.target.value}`
// });

// $('#project').on('change', event => {
//   window.location = `/total-hours-by-project/${event.target.value}${window.location.search}`;
// });

// $('#staff').on('change', event => {
//   window.location = `/percentage/${event.target.value}${window.location.search}`;
// });
